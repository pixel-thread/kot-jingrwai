---
description: Update Protocol
---

# /update-protocol

1. Review the last 10 entries in `memory/evolution.md`.
2. Identify any recurring patterns (e.g., "Always adding this type of helper").
3. Suggest an update to `GLOBAL.md` or a new `rule/*.md` to automate that pattern.
